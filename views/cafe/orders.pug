extends ../layouts/main-layout.pug

block content
  main
    if orders.length > 0
      ul
        each order in orders
          li 
            h1 #{order.id} 
            ul
              each item in order.products
                li #{item.name} #{item.orderItem.quantity}
