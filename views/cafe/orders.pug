extends ../layouts/main-layout.pug

block content
  main
    if orders.length > 0
      ul
        each order in orders
          li 
            h1 #{order.id} 
            ul
              each p in order.products
                li #{p.product.name} #{p.product.quantity} - 
                  a(href=`/invoice/${order.id}`) Invoice
