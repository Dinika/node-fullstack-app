extends ../layouts/main-layout.pug

block content
  main.center
    h1 #{product.name}
    hr
    img(src=(product.imageUrl), alt="presentation" width='300px')
    h2 #{product.price}
    p #{product.description}
    form(action="/card", method="POST")
        button.btn Add to cart